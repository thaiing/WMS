<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiruantong.system.mapper.tenant.SysTenantMenuMapper">

  <resultMap type="com.yiruantong.system.domain.core.SysMenu" id="SysMenuResult">
  </resultMap>

  <select id="selectMenuByUserId" parameterType="Long" resultMap="SysMenuResult">
    select distinct m.menu_id,
                    m.parent_id,
                    m.menu_name,
                    m.path,
                    m.component,
                    m.component_name,
                    m.query_param,
                    m.visible,
                    m.perms,
                    m.is_frame,
                    m.is_cache,
                    m.is_link,
                    m.menu_type,
                    m.icon,
                    m.order_num,
                    m.create_time,
                    m.dynamic_path
    from sys_tenant_menu m
           left join sys_role_auth rm on m.menu_id = rm.menu_id
           left join sys_user_role sur on rm.role_id = sur.role_id
           left join sys_role ro on sur.role_id = ro.role_id
           left join sys_user u on sur.user_id = u.user_id
    where u.user_id = #{userId}
      AND m.`package_id` = #{packageId}
      AND m.`enable` = 1
      AND ro.`enable` = 1
      AND rm.auth_value like '%=1%'
    order by m.parent_id, m.order_num DESC, m.menu_id
  </select>
</mapper>
